// JavaScript Document
$('#banner-me').owlCarousel({
   	 loop:true,	// cho lap lại
	items:1,	// xuất hiện 1 ảnh thôi
	smartSpeed:1000,   	// tốc độ thay đổi ảnh
	autoplay:true,		// cho phép tự động chạy
    	autoplayTimeout:5000,  // thời gian chờ khi chuyển ảnh khi chạy tự động
nav:true,	// cho xuất hiện bộ nút tới lui 
	  })
$('#tes').owlCarousel({ loop:true, margin:80, responsive:{ 0:{ items:1, 		 }, 600:{ items:2, 			dots:false, 			nav:true, }, 1000:{ items:3 } } })
 
$('#banner-danhgia').owlCarousel({
   	 loop:true,	// cho lap lại
	items:1,	// xuất hiện 1 ảnh thôi
	smartSpeed:1000,   	// tốc độ thay đổi ảnh
	autoplay:true,		// cho phép tự động chạy
    	autoplayTimeout:5000,  // thời gian chờ khi chuyển ảnh khi chạy tự động
nav:true,	// cho xuất hiện bộ nút tới lui 
	  })
$('#tes').owlCarousel({ loop:true, margin:80, responsive:{ 0:{ items:1, 		 }, 600:{ items:2, 			dots:false, // JavaScript Document